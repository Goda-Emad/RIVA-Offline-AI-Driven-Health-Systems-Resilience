# ============================================================
# RIVA Health Platform — Production Requirements
# ai-core/requirements-prod.txt
# ============================================================
# بيئة النشر على Vercel / Docker — بدون torch (700MB+)
# الـ inference بيتم عبر ONNX Runtime فقط
#
# التثبيت:
#   pip install -r requirements-prod.txt
#   أو بـ uv (أسرع بـ 10-100x):
#   uv pip install -r requirements-prod.txt
#
# التحقق من الثغرات قبل النشر:
#   safety check -r requirements-prod.txt
# ============================================================

# ── ONNX Inference (بديل torch في Production) ────────────────
onnxruntime==1.17.3
onnx==1.16.1
numpy==1.26.4

# ── Whisper / STT ────────────────────────────────────────────
openai-whisper==20231117
soundfile==0.12.1
librosa==0.10.1
sounddevice==0.4.6

# ── Transformers / Tokenizer ──────────────────────────────────
transformers==4.40.2
tokenizers==0.19.1

# ── Triage / Readmission / LOS (XGBoost + sklearn) ───────────
xgboost==2.0.3
scikit-learn==1.4.2
joblib==1.4.0

# ── Explainability ───────────────────────────────────────────
shap==0.45.1
plotly==5.22.0

# ── Data processing ──────────────────────────────────────────
pandas==2.2.2
scipy==1.13.0
imbalanced-learn==0.12.3

# ── Security / Encryption ────────────────────────────────────
cryptography==42.0.8
PyNaCl==1.5.0

# ── FHIR / HL7 (متوافق مع FastAPI عبر Pydantic) ─────────────
fhir.resources==7.1.0
hl7apy==1.3.4

# ── QR Code ──────────────────────────────────────────────────
qrcode==7.4.2
Pillow==10.3.0

# ── Cloud Storage / Alerts ───────────────────────────────────
httpx==0.27.0

# ── Compression / Offline ────────────────────────────────────
zstandard==0.22.0

# ── FastAPI (web layer) ───────────────────────────────────────
fastapi==0.111.0
uvicorn==0.29.0
python-multipart==0.0.9
pydantic==2.7.1

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1
tqdm==4.66.4
colorlog==6.8.2

# ── Security scanning (يُشغَّل في CI قبل النشر) ──────────────
safety==3.2.3
