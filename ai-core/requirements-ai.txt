# ============================================================
# RIVA Health Platform — AI Core Requirements
# ai-core/requirements-ai.txt
# ============================================================
# تثبيت: pip install -r requirements-ai.txt --break-system-packages
# ============================================================

# ── Core ML / ONNX ───────────────────────────────────────────
onnx>=1.16.0
onnxruntime>=1.17.0          # CPU inference — whisper + chatbot + triage
torch>=2.2.0                 # model export + training
numpy>=1.26.0

# ── Whisper / Speech ─────────────────────────────────────────
openai-whisper>=20231117     # STT + mel spectrogram
soundfile>=0.12.1            # audio I/O (wav/flac/ogg)
librosa>=0.10.1              # audio resampling
sounddevice>=0.4.6           # mic recording (optional)

# ── Transformers / Tokenizer ──────────────────────────────────
transformers>=4.40.0         # WhisperTokenizer + AutoTokenizer
tokenizers>=0.19.0

# ── Triage model (XGBoost) ───────────────────────────────────
xgboost>=2.0.3
scikit-learn>=1.4.0          # preprocessing + evaluation
joblib>=1.3.0                # model serialisation (.pkl)

# ── Pregnancy model (Random Forest) ──────────────────────────
# scikit-learn covers this — no extra dep needed

# ── School model (K-Means) ───────────────────────────────────
# scikit-learn covers this

# ── Readmission / LOS models ─────────────────────────────────
# xgboost + scikit-learn cover these

# ── Explainability ───────────────────────────────────────────
shap>=0.45.0                 # SHAP values for AI explanation page
plotly>=5.20.0               # spider charts + SHAP visualisation

# ── Data processing ──────────────────────────────────────────
pandas>=2.2.0
scipy>=1.13.0
imbalanced-learn>=0.12.0     # SMOTE for class imbalance

# ── Security / Encryption ────────────────────────────────────
cryptography>=42.0.0         # AES-256-GCM (encryption_handler.py)
PyNaCl>=1.5.0                # digital signatures

# ── FHIR / HL7 Interoperability ──────────────────────────────
fhir.resources>=7.1.0        # FHIR R4 resource models
hl7apy>=1.3.4                # HL7 v2 parsing

# ── QR Code ──────────────────────────────────────────────────
qrcode[pil]>=7.4.2           # QR generation for result page
Pillow>=10.3.0

# ── Cloud Storage / Alerts ───────────────────────────────────
httpx>=0.27.0                # Supabase + Vercel KV + Slack alerts
                             # (doctor_feedback_handler + clinical_override_log)

# ── Drug interaction ─────────────────────────────────────────
# No external dep — uses local drug_conflicts.json

# ── Model quantisation ───────────────────────────────────────
onnxruntime-tools>=1.7.0     # ONNX optimisation helpers

# ── Compression / Offline ────────────────────────────────────
zstandard>=0.22.0            # data_compressor.py

# ── Sentiment analysis ───────────────────────────────────────
# Uses local DistilBERT ONNX — no extra dep

# ── Testing ──────────────────────────────────────────────────
pytest>=8.1.0
pytest-asyncio>=0.23.0

# ── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.1         # .env loading
tqdm>=4.66.0                 # progress bars during training
colorlog>=6.8.0              # coloured logging
