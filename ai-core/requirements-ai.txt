# ============================================================
# RIVA Health Platform — AI Requirements (Master)
# ai-core/requirements-ai.txt
# ============================================================
#
# ملف رئيسي — اقرأ التعليمات وانتقل للملف المناسب:
#
#   ┌─────────────────────────────────────────────────────┐
#   │  النشر على Vercel / Docker:                         │
#   │  uv pip install -r requirements-prod.txt            │
#   │  (بدون torch — حجم ~120MB)                          │
#   ├─────────────────────────────────────────────────────┤
#   │  التدريب المحلي / Google Colab:                     │
#   │  uv pip install -r requirements-train.txt           │
#   │  (مع torch — حجم ~900MB)                            │
#   └─────────────────────────────────────────────────────┘
#
# لماذا uv؟
#   pip install torch scikit-learn transformers → ~8 دقائق
#   uv pip install torch scikit-learn transformers → ~40 ثانية
#
#   تثبيت uv:
#   curl -LsSf https://astral.sh/uv/install.sh | sh
#   أو: pip install uv
#
# التحقق من الثغرات قبل النشر:
#   safety check -r requirements-prod.txt
#   bandit -r ai-core/ -ll
#
# ============================================================
#
# لو عايز تثبت كل حاجة دفعة واحدة (dev فقط):
-r requirements-train.txt
